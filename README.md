# Assignment #1: HTML & CSS Basics — Report

**Name:** Isaali Abdunabi  
**Group:** IT-2502  
**GitHub Repository Link:** [https://github.com/AvgustIS/Web-Homework-1](https://github.com/AvgustIS/Web-Homework-1)

---

## Objective
The objective of this assignment is to understand the core structure and purpose of HTML, implement basic and intermediate tags, style the webpage using inline, internal, and external CSS, utilize layout mechanics such as the box model and positioning, and finally publish the functional project online via GitHub Pages.

---

## Description of Steps Taken & Implementation

### Part 1: Introduction to HTML
* **Step 0 & 1:** Created the main `index.html` file using the standard HTML boilerplate (`<!DOCTYPE html>`, `<html>`, `<head>`, `<title>`, `<body>`). Set the page title to *"My First Webpage"* and added headings (`<h1>` to `<h3>`) featuring my name, course, and an "About Me" introductory section, accompanied by a descriptive paragraph (`<p>`).
* **Step 2:** Added structured list elements, including an ordered list (`<ol>`) outlining my personal hobbies and an unordered list (`<ul>`) featuring my favorite websites.
* **Step 3 & 4:** Inserted a personal photograph using the `<img>` tag and added external links using `<a>` tags pointing to my preferred websites. Included a static HTML button with the text *"Click Me"*.

### Part 2: Intermediate HTML
* **Step 5:** Built a structured weekly class schedule table utilizing table tags (`<table>`, `<tr>`, `<th>`, `<td>`) divided into three distinct columns: `"Subject"`, `"Day"`, and `"Time"`.
* **Step 7 & 8:** Incorporated emojis inside a paragraph reflecting my current mood and constructed an interactive HTML form containing fields for user name, email address, a color picker (`<input type="color">`), and a submit button.

### Part 3: Introduction to CSS
* **Step 9 to 12:** Applied styling using multiple approaches. Integrated inline styling (`style="..."`) on specific text blocks, added internal CSS rules inside the `<head>` section, and organized production styles into an external stylesheet named `style.css`, linked properly via `<link rel="stylesheet" href="style.css">`.
* **Step 13 & 14:** Practiced CSS syntax and hierarchy by implementing element selectors (`p`), a class selector (`.highlight`) for styled sections, and an ID selector (`#main-heading`) to uniquely target the primary header.

### Part 4: Intermediate CSS
* **Step 15 to 17:** Added a custom favicon link to the header (`<link rel="icon">`), grouped layout blocks using `<div>` elements for structural components (header, main content, footer), and applied the CSS Box Model properties (borders, padding, and margins) to manage spacing cleanly.
* **Step 18 to 20:** Experimented with CSS positioning techniques (static, relative, and absolute), varied sizing units (`px`, `rem`), and established side-by-side content containers using `float` paired with `clear` to prevent layout overlapping.
* **Step 21:** Successfully deployed and published the complete webpage live online using GitHub Pages.

---

## Screenshots

* **Main Page Structure & Heading Section**  
  <img width="1903" height="894" alt="Screenshot 2026-09-14 184912" src="https://github.com/user-attachments/assets/4949b1e7-3438-4c38-97c8-28abd8f8347b" />


* **Hobbies List, Schedule Table, and Form AND CSS Styling, Div Layouts & Box Model**  
<img width="1892" height="890" alt="Screenshot 2026-09-14 184948" src="https://github.com/user-attachments/assets/2172c81b-0c38-46ab-9d75-413fa0a9c8c4" />
<img width="1901" height="888" alt="Screenshot 2026-09-14 184935" src="https://github.com/user-attachments/assets/c5b966fe-78c3-4f3a-a57d-20d7868b033b" />


---

## Brief Summary of Work Process
The development process started by establishing a local repository structure for `Web-Homework-1` and writing the foundational HTML tags inside `index.html`. After building out structural sections like tables, lists, and forms, I created an external `style.css` file to design the interface layout. I utilized classes, IDs, the box model, and floating properties to refine the visual presentation. Finally, version control was handled via Git, pushing the completed codebase to GitHub and enabling GitHub Pages for public deployment.
